# CryptoTax Wallet — Калькулятор налогов для TON кошельков

**CryptoTax Wallet** — это веб-приложение для автоматического расчета налогов на криптовалютные транзакции в сети TON (The Open Network). Приложение позволяет пользователям подключать свои TON кошельки через TON Connect, просматривать историю транзакций и автоматически рассчитывать налоговые обязательства по методу FIFO (First In, First Out).

Проект разработан для упрощения процесса налоговой отчетности для владельцев криптовалютных кошельков. Система автоматически определяет прибыль от продажи TON и рассчитывает налог в размере 5% от прибыли, отображая результаты как в USD, так и в TON.

## Features

- 🔐 **Аутентификация пользователей** — регистрация и вход с использованием JWT токенов
- 💼 **Подключение TON кошельков** — интеграция с TON Connect для безопасного подключения кошельков (Tonkeeper и другие)
- 📊 **Просмотр транзакций** — автоматическая загрузка и отображение истории транзакций из блокчейна TON
- 💰 **Расчет налогов** — автоматический расчет налогов по методу FIFO с учетом покупок и продаж
- 📈 **Помесячный анализ** — детальный расчет налогов за каждый месяц с возможностью просмотра за весь период
- 💵 **Конвертация в USD** — автоматическое получение курсов TON/USD через CoinGecko API для расчета налогов в долларах
- 🎨 **Современный UI** — темная тема в стиле криптокошельков с адаптивным дизайном
- 🔄 **Кэширование данных** — оптимизация производительности через кэширование транзакций в базе данных
- 📱 **SPA архитектура** — одностраничное приложение с навигацией без перезагрузки страницы

## Tech Stack

### Backend
- **Python 3.x** — основной язык программирования
- **Django 5.2.6** — веб-фреймворк
- **Django REST Framework 3.16.1** — для создания REST API
- **PyJWT 2.10.1** — для JWT аутентификации
- **pytoniq-core 0.1.45** — библиотека для работы с блокчейном TON
- **pytonconnect 0.3.2** — интеграция с TON Connect
- **requests 2.32.5** — для HTTP запросов к внешним API
- **aiohttp 3.13.2** — асинхронные HTTP запросы для работы с TON блокчейном

### Frontend
- **Vanilla JavaScript (ES6+)** — без использования фреймворков
- **HTML5 / CSS3** — разметка и стилизация
- **TON Connect SDK** — для подключения кошельков

### Database
- **SQLite** — база данных для разработки (легко мигрировать на PostgreSQL/MySQL)

### External APIs
- **CoinGecko API** — получение текущих и исторических курсов TON/USD
- **TON Blockchain** — получение данных о транзакциях и балансе кошельков

### Development Tools
- **django-cors-headers** — для CORS в разработке
- **django-unfold** — улучшенный интерфейс админ-панели

## Installation

### Предварительные требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Git (для клонирования репозитория)

### Пошаговая установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd wallet_project/wallet
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   
   На Windows:
   ```bash
   venv\Scripts\activate
   ```
   
   На Linux/Mac:
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Выполните миграции базы данных:**
   ```bash
   python manage.py migrate
   ```

6. **Создайте суперпользователя (опционально, для доступа к админ-панели):**
   ```bash
   python manage.py createsuperuser
   ```
   Введите email и пароль при запросе.

7. **Запустите сервер разработки:**
   ```bash
   python manage.py runserver
   ```

8. **Откройте приложение в браузере:**
   - Основное приложение: `http://localhost:8000/` или `http://127.0.0.1:8000/`
   - Админ-панель: `http://localhost:8000/admin/`

### Настройка для разработки с ngrok (опционально)

Если нужно тестировать TON Connect с мобильного устройства:

1. Установите ngrok: https://ngrok.com/
2. Запустите ngrok туннель:
   ```bash
   ngrok http 8000
   ```
3. Обновите `CSRF_TRUSTED_ORIGINS` в `wallet/settings.py` с вашим ngrok доменом

## Screenshots

### Главная страница (Dashboard)
![Dashboard](docs/screenshots/dashboard.png)
*Главная страница с информацией о балансе кошелька, количестве транзакций и статусе подключения*

### История транзакций
![Transactions](docs/screenshots/transactions.png)
*Список всех транзакций с визуальной индикацией входящих (зеленый) и исходящих (красный) операций*

### Расчет налогов
![Tax Calculation](docs/screenshots/tax.png)
*Интерфейс расчета налогов с возможностью выбора периода и просмотра детальной информации*

### Подключение кошелька
![Wallet Connection](docs/screenshots/wallet_connect.png)
*Процесс подключения TON кошелька через TON Connect*

### Помесячный анализ налогов
![Monthly Tax](docs/screenshots/monthly_tax.png)
*Детальный помесячный анализ налоговых обязательств*

## Database Schema (ER-диаграмма)

ER-диаграмма базы данных находится в файле `docs/er_diagram.png` или `docs/er_diagram.svg`.

### Основные модели:

- **User** — пользователи системы (email, пароль, связь с кошельком)
- **WalletSession** — сессии подключенных кошельков (адрес, тип кошелька, статус подключения)
- **TransactionHistory** — история транзакций (хэш, сумма, адреса отправителя/получателя, время)

### Связи:
- User (1) ←→ (1) WalletSession
- WalletSession (1) ←→ (N) TransactionHistory (через wallet_address)

## API Documentation

### Базовый URL
```
http://localhost:8000/api/
```

### Endpoints

#### Аутентификация

**POST `/api/register/`** — Регистрация нового пользователя
- **Request Body:**
  ```json
  {
    "email": "user@example.com",
    "password": "securepassword"
  }
  ```
- **Response:** `201 Created`
  ```json
  {
    "id": 1,
    "email": "user@example.com",
    "tokens": {
      "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
      "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
    }
  }
  ```

**POST `/api/login/`** — Вход в систему
- **Request Body:**
  ```json
  {
    "email": "user@example.com",
    "password": "securepassword"
  }
  ```
- **Response:** `200 OK` (аналогично регистрации)

**POST `/api/refresh/`** — Обновление access токена
- **Request Body:**
  ```json
  {
    "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGc..."
  }
  ```
- **Response:** `200 OK`
  ```json
  {
    "tokens": {
      "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
      "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
    }
  }
  ```

#### Управление кошельком

**GET `/api/Wallet/`** — Получить информацию о подключенном кошельке
- **Headers:** `Authorization: Bearer <access_token>`
- **Response:** `200 OK`
  ```json
  {
    "id": 1,
    "wallet_address": "UQAbc123...",
    "wallet_type": "tonkeeper",
    "connected": true
  }
  ```

**POST `/api/Wallet/`** — Подключить кошелек
- **Headers:** `Authorization: Bearer <access_token>`
- **Request Body:**
  ```json
  {
    "wallet_address": "UQAbc123...",
    "wallet_type": "tonkeeper"
  }
  ```
- **Response:** `200 OK`

**PUT `/api/Wallet/`** — Отключить кошелек
- **Headers:** `Authorization: Bearer <access_token>`
- **Request Body:**
  ```json
  {
    "connected": false
  }
  ```
- **Response:** `200 OK`

#### Транзакции и баланс

**GET `/api/wallet/balance/`** — Получить баланс кошелька
- **Headers:** `Authorization: Bearer <access_token>`
- **Response:** `200 OK`
  ```json
  {
    "balance": "100.5",
    "balance_ton": "100.5",
    "is_active": true
  }
  ```

**GET `/api/wallet/transactions/?refresh=true`** — Получить историю транзакций
- **Headers:** `Authorization: Bearer <access_token>`
- **Query Parameters:**
  - `refresh` (optional): `true` для принудительного обновления из блокчейна
- **Response:** `200 OK`
  ```json
  {
    "transactions": [
      {
        "tx_hash": "abc123...",
        "timestamp": "2025-12-22T19:59:37Z",
        "amount": 4.994116795,
        "amount_ton": "4.994116795",
        "amount_usd": 24.97,
        "from_address": "UQAbc123...",
        "to_address": "UQDef456...",
        "status": "completed"
      }
    ],
    "count": 10,
    "from_cache": true
  }
  ```

#### Расчет налогов

**GET `/api/tax/month/?year=2025&month=12`** — Расчет налога за месяц
- **Headers:** `Authorization: Bearer <access_token>`
- **Query Parameters:**
  - `year` (required): Год (например, 2025)
  - `month` (required): Месяц (1-12)
- **Response:** `200 OK`
  ```json
  {
    "year": 2025,
    "month": 12,
    "total_sent_ton": 15.0,
    "total_sent_usd": 75.0,
    "total_tax_ton": 0.5,
    "total_tax_usd": 2.5,
    "transactions_count": 5,
    "transactions": [...],
    "demo_deals": [...]
  }
  ```

**GET `/api/tax/all/?start_year=2025&start_month=8`** — Расчет налогов за все месяцы
- **Headers:** `Authorization: Bearer <access_token>`
- **Query Parameters:**
  - `start_year` (optional): Начальный год
  - `start_month` (optional): Начальный месяц
- **Response:** `200 OK`
  ```json
  {
    "monthly_taxes": [
      {
        "year": 2025,
        "month": 8,
        "total_tax_usd": 10.5,
        ...
      }
    ]
  }
  ```

**GET `/api/tax/total/?start_year=2025&start_month=8`** — Итоговый расчет налогов
- **Headers:** `Authorization: Bearer <access_token>`
- **Query Parameters:**
  - `start_year` (optional): Начальный год
  - `start_month` (optional): Начальный месяц
- **Response:** `200 OK`
  ```json
  {
    "total_tax_ton": 2.5,
    "total_tax_usd": 12.5,
    "total_sent_ton": 50.0,
    "total_sent_usd": 250.0,
    "total_transactions": 20,
    "ton_price_usd": 5.0,
    "period": {
      "start": "2025-08",
      "end": "2025-12"
    },
    "monthly_taxes": [...]
  }
  ```

### Postman Collection

Экспортированная коллекция Postman доступна в файле `docs/postman_collection.json`.

## Архитектурная схема

```
┌─────────────┐
│   Browser   │
│  (Frontend) │
└──────┬──────┘
       │ HTTP/HTTPS
       │ (REST API)
       ▼
┌─────────────────────────────────┐
│      Django Backend              │
│  ┌──────────────────────────┐   │
│  │   Django REST Framework  │   │
│  │   (API Endpoints)        │   │
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │   Business Logic          │   │
│  │   - Tax Calculator        │   │
│  │   - TON Service          │   │
│  └──────────────────────────┘   │
└──────┬──────────────────┬───────┘
       │                  │
       │                  │
       ▼                  ▼
┌─────────────┐   ┌──────────────┐
│   SQLite    │   │  TON          │
│  Database   │   │  Blockchain   │
│             │   │  (pytoniq)    │
└─────────────┘   └───────┬───────┘
                          │
                          ▼
                   ┌──────────────┐
                   │  CoinGecko   │
                   │     API      │
                   │  (TON/USD)   │
                   └──────────────┘
```

### Описание компонентов:

1. **Browser (Frontend)** — одностраничное приложение на чистом JavaScript, взаимодействует с бэкендом через REST API
2. **Django Backend** — серверная часть приложения:
   - **Django REST Framework** — обработка HTTP запросов и маршрутизация
   - **Business Logic** — основная логика приложения (расчет налогов, работа с блокчейном)
3. **SQLite Database** — хранение пользователей, сессий кошельков и кэшированных транзакций
4. **TON Blockchain** — получение данных о транзакциях и балансе через библиотеку pytoniq
5. **CoinGecko API** — получение актуальных и исторических курсов TON/USD для расчета налогов

## Code Documentation

### Основные модули

#### `wallet_nalog/models.py`
Содержит модели данных:
- **User** — модель пользователя с JWT токенами
- **WalletSession** — модель сессии кошелька
- **TransactionHistory** — модель истории транзакций

#### `wallet_nalog/views.py`
Содержит API endpoints и бизнес-логику:
- `Registration`, `Login`, `RefreshToken` — аутентификация
- `connect_wallet` — управление подключением кошелька
- `get_wallet_balance`, `get_wallet_transactions` — работа с кошельком
- `get_tax_for_month`, `get_tax_for_all_months`, `get_total_tax` — расчет налогов

#### `wallet_nalog/tax_calculator.py`
Модуль расчета налогов:
- `calculate_tax_for_month()` — расчет налога за конкретный месяц по методу FIFO
- `calculate_tax_for_all_months()` — расчет налогов за все месяцы
- `calculate_total_tax()` — итоговый расчет налогов
- `get_ton_price_for_date()` — получение исторической цены TON

#### `wallet_nalog/tonservice.py`
Сервис для работы с блокчейном TON:
- `get_history_transaction()` — получение истории транзакций из блокчейна
- `account_info()` — получение информации о балансе кошелька
- `save_transactions_to_db()` — сохранение транзакций в БД

#### `wallet_nalog/static/wallet/js/app.js`
Основная логика фронтенда:
- `initTonConnect()` — инициализация TON Connect
- `loadTransactions()` — загрузка и отображение транзакций
- `loadTaxForMonth()` — загрузка расчета налогов
- `createTransactionElement()` — создание элементов транзакций

### Docstrings

Все ключевые функции и классы содержат docstrings на русском языке, описывающие их назначение, параметры и возвращаемые значения. Примеры:

```python
def calculate_tax_for_month(wallet_address, year, month, ton_price_usd=None):
    """
    Расчёт налога за месяц по логике:
    - считаем покупки и продажи TON;
    - для каждой продажи считаем прибыль = сумма продажи - сумма покупок (FIFO),
      использованных под эту продажу;
    - если прибыль > 0, налог = 5% от прибыли;
    - если продажа "в минус" (прибыль <= 0), налог не берётся.
    """
```

## Дополнительная информация

### Структура проекта

```
wallet_project/
├── wallet/
│   ├── wallet/              # Настройки Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── ...
│   ├── wallet_nalog/        # Основное приложение
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── tax_calculator.py
│   │   ├── tonservice.py
│   │   ├── serializers.py
│   │   ├── urls.py
│   │   ├── templates/
│   │   └── static/
│   ├── manage.py
│   ├── requirements.txt
│   └── db.sqlite3
├── docs/                    # Документация
│   ├── er_diagram.png
│   ├── postman_collection.json
│   └── screenshots/
└── README.md
```

### Лицензия

Этот проект создан в рамках учебной практики.
